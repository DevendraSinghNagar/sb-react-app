# React

    git init
    npm init
    README.md
    .gitignore

# Package

what is ^ and ~
npm i parcel
npm i react
npm i react-dom
browserlist

# Parcel

    npx parcel index.html

# Run/Build

    npm start/npm run start
    npm run build

# Redux

    npm i npm install @reduxjs/toolkit react-redux

# React Testing Library

    1 react-testing-library: npm install --save-dev @testing-library/react @testing-library/dom
    2 jest(https://jestjs.io/docs/getting-started): a) npm install --save-dev jest
    b) npm install --save-dev babel-jest @babel/core @babel/preset-env
    c) babel.config.js
    3 parcel(https://parceljs.org/languages/javascript/): .parcelrc:
    4 Jest configuration: npm init jest@latest
    5 jsdom: npm install --save-dev jest-environment-jsdom
    6 npm i -D @babel/preset-react and configure in babel.config file
    7 npm i -D @testing-library/jest-dom
